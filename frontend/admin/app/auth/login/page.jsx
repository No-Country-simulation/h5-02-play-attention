import { Login } from '@/features/auth';

export const metadata = {
  title: 'Iniciar sesión | Play Attention Admin',
  description: 'Iniciar sesión en el panel de administración de Play Attention'
};

export default function LoginPage() {
  return <Login />;
}
