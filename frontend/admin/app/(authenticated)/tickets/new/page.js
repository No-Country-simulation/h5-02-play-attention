import CreateTicket from '@/features/tickets/components/CreateTicket';

/**
 * Página de Creación de Tickets de Soporte
 * Siguiendo el principio de Responsabilidad Única (SRP), esta página solo se encarga
 * de renderizar el componente principal de creación de tickets
 */
export default function CreateTicketPage() {
  return <CreateTicket />;
}
